# Core requirements
pip install cryptography docker pyinstrument

# Optional: Better parsing
pip install tree-sitter antlr4-python3-runtime

# Optional: Docker (for safe execution)
# Follow instructions at https://docs.docker.com/get-docker/