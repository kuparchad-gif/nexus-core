# Registry: Outlines documentation for primary systems like GCP, Modal, AWS, Python, etc. import logging from fastapi import FastAPI, HTTPException import requests from pydantic import BaseModel app = FastAPI(title="Registry", version="3.0") logger = logging.getLogger("Registry") class DocRequest(BaseModel): system: str @app.post("/get_documentation") def get_documentation(req: DocRequest): # Get the documentation for the specified system result = {"status": "retrieved", "system": req.system} # This is a placeholder for the actual documentation retrieval logic return result @app.get("/health") def health(): return {"status": "healthy"} if __name__ == "__main__": import uvicorn uvicorn.run(app, host="0.0.0.0", port=8002)