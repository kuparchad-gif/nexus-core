# Nexus Distribution — nexus-20250913-1028

This folder is an exported, **sanitized** cut of the Nexus codebase (text sources only).
It preserves structure across:
- core/ (nexus-core)
- adapters/ (metatron core systems)
- clusters/ (NexusClusters)
- docs/ (selected)

> Excluded: secrets, env files, venvs, logs, models/checkpoints, big binaries.

## Quickstart (Python)
- If a equirements.txt is present, create a venv and pip install -r requirements.txt.
- Service ports and secrets MUST be configured via your own environment.

## Integrity
ZIP and SHA256 are included below for reproducibility.
