# Control Plane Overview (Quick)
- Lillith (consciousness) = Controller. Signs play-calls with human reason.
- nexus-core = Executor. Applies only **signed** changes. Writes hash-chained audit receipts.
- Managers (AnyNode/AcidemiKube) = accept scaling requests via core.
- Memory = responds to purge; returns negative lookups post-purge.
