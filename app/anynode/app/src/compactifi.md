# Compactifi Runtime — CPU‑First Micro‑Cells

Compactifi turns each CogniKube into *many* tiny “cells” by cycling compute and memory aggressively on CPU first.
Tune via env or `nexus-core/compactifi/config/defaults.yaml`.

Ports: LP 7101, Memory 7102
[REDACTED-SECRET-LINE]

